

const Home = () => {
    // const navigate = useNavigate()
    // const {isAuthentify} = useAuth()
    // useEffect(() => {
    //     if (!isAuthentify) {
    //         navigate('/login')
    //     }
    // }, [isAuthentify, navigate])
    return (
        <div> welcome Home</div>
    )
}

export default Home